---
import { getEntry } from "astro:content"
import EntryLayout from "@components/company/EntryLayout.astro"

// Use Astro's built-in API
const entry = await getEntry("company", "engage")

if (!entry) {
  throw new Error(`Content entry not found for 'company/engage'`)
}
---

<EntryLayout entry={entry} />
