---
import { getEntry } from "astro:content"
import EntryLayout from "@components/company/EntryLayout.astro"

// Use Astro's built-in API
const entry = await getEntry("company", "about")

if (!entry) {
  throw new Error(`Content entry not found for 'company/about'`)
}
---

<EntryLayout entry={entry} />
